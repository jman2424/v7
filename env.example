# Core Mode
MODE=V7
BUSINESS_KEY=EXAMPLE

# Optional AI provider. Business setup, widget preview, and model choices work without a key.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OPENAI_TEMPERATURE=0.3
OPENAI_TIMEOUT=30

# Server
PORT=10000
SECRET_KEY=
# Required: generate a random value of at least 32 characters before starting.
BASE_URL=http://127.0.0.1:10000
ENVIRONMENT=development
LOG_LEVEL=info

# WhatsApp
WHATSAPP_VERIFY_TOKEN=
WHATSAPP_APP_SECRET=
WHATSAPP_TOKEN=
WHATSAPP_PHONE_ID=
WHATSAPP_API_URL=https://graph.facebook.com/v21.0
# Optional for multiple businesses on one WhatsApp Cloud app. Keep this mapping server-only.
# WHATSAPP_TENANT_MAP_JSON={"123456789012345":"EXAMPLE","987654321098765":"NORTHSTAR"}
# Required in production when accepting Twilio WhatsApp webhooks.
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_NUMBER=

# Google Sheets
SHEETS_API_KEY=your_key
SHEETS_ANALYTICS_SHEET_ID=sheet_id
SHEETS_EXPORT_SHEET_ID=sheet_id

# Billing
BILLING_PROVIDER=stripe
STRIPE_API_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_API_BASE=https://api.stripe.com
# Existing Stripe Tax Rate ID: active, 20%, exclusive (not inclusive).
STRIPE_TAX_RATE_ID=
# Mount persistent storage for SECURITY_DB_PATH and V7_DATA_DIR before live billing.

# Admin Auth
ADMIN_USERNAME=
ADMIN_PASSWORD=
ADMIN_PASSWORD_HASH=
ADMIN_TOTP_SECRET=
# Prefer scripts/manage_account.py and a protected registry outside business/.
ADMIN_USERS_FILE=
SECURITY_DB_PATH=logs/security.db
# Tenant owners use bcrypt password hashes, never plaintext passwords.
# BUSINESS_USERS_JSON=[{"email":"owner@example.com","tenant":"EXAMPLE","password_hash":"$2b$...","roles":["business_owner"]}]

# Optional Integrations
MAPS_API_URL=https://maps.googleapis.com/maps/api/geocode/json
MAPS_API_KEY=your_key
MAIL_API_URL=https://api.mailgun.net/v3/...
MAIL_API_KEY=key-...
MAIL_FROM=no-reply@example.com
# Public signup stays disabled until all four SMTP values are configured.
SMTP_HOST=
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=
SMTP_TLS_MODE=starttls
SMTP_PORT=587

# Optional owner OAuth for MCP and REST; HTTPS origin must match BASE_URL.
MCP_PUBLIC_URL=
MCP_OAUTH_CLIENTS={}
MCP_ALLOWED_ORIGINS=
