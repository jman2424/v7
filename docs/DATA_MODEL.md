# Data Model and Schemas

## Overview
All tenant-specific data is stored in the `business/` directory and validated against JSON schemas located in `schemas/`.  
Each tenant folder mirrors the same structure and can be snapshotted for version control.

---

## Tenant Folder Layout
